#!/usr/bin/env python3
from math import ceil

def get_mean(array1, array2):
    """TODO: Do the thing.
    """
    # mean_index = ceil((len(array1)+len(array2))/2)
    median = 0
    array1_index = 0
    array2_index = 0

    
    
    return 0

if __name__ == "__main__":
    array1 = [1, 2, 3, 4, 5]
    array2 = [6, 7, 8, 9]

    mean = get_mean(array1, array2)

    print("The mean of: \n%s\nand\n%sis: %s"%(array1, array2, mean))